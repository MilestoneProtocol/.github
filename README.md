# .github
GrantOS v3 is the first onchain grant protocol that enforces milestone delivery with cryptographic proofs not trust, not screenshots, not committee opinion. The contract verifies. The code runs.
